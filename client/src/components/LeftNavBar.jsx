import React from 'react';

const LeftNavBar = (props) => {
  const Links = [
    'SHOP',
    'GALLERY',
    'STOCKISTS',
    'STORIES',
    'HELP'
  ];

  let LinkButtons = (linksArray) => {

  };

  return LinkButtons;
};

export default LeftNavBar;